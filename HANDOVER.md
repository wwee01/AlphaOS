# HANDOVER

**Checkpoint: 2026-07-03 (post PR2.6 merge) · branch `main` @ `0918a55` · tests 442 passed / 3 skipped / 0 failed · working tree clean · mode PAPER · execution `alpaca_paper` · AI = LIVE (OpenAI) · real-money UNREACHABLE · 0 open positions**

> Single entry point for the next session. This project keeps no other handover docs — everything is here. Verify state before trusting any of it (commands in §8).
> ✅ **RESOLVED — protection watchdog is built, merged, AND hardened** (PR2.5 → PR #21, PR2.6 → PR #24). The META incident's root cause (day-TIF bracket legs expiring at session close, no watchdog to notice) is fixed at the source (swing holds now submit GTC protective legs by default) AND backstopped by a watchdog that detects missing protection, a broker-closed/local-open mismatch, or an unverifiable broker-lookup failure every monitor pass, and blocks all new entries until resolved. Three separate Opus audits (PR2.5 initial, PR2.5 narrow HIGH-1 re-audit, PR2.6) all concluded **approve**. A handful of LOW/cosmetic follow-ups remain (§4/§7); nothing blocks current safety.
> ✅ **RESOLVED — the pre-existing flaky test is merged.** `tests/test_mfe_mae.py`'s date-dependent price-probe issue (unrelated to the watchdog work, discovered during post-PR2.5 verification) is fixed and merged (PR #22). `main`'s suite is clean.

## Changelog (most recent first)
- **PR2.6 — Protection watchdog hardening** (`main` @ `0918a55`, PR #24, this checkpoint). Follow-up hardening on PR2.5 per its own Opus audit findings, done before PR3 scheduler as recommended. **check_error fail-louder/fail-closed**: a single broker per-order lookup failure stays WARNING-only (now actually logged — previously silently wrote an audit row with no alert at all) and does not block; after `PROTECTION_CHECK_ERROR_ESCALATION_THRESHOLD` (default 3, validated 1–10 at settings load) *consecutive* failures for the same position, escalates to a new `unverifiable` status — CRITICAL, blocks all new entries exactly like `unprotected`. Self-heals the instant a lookup succeeds; verified a stale `protection_ack` cannot durably unblock an ongoing failure (re-escalates the very next pass). **Qty mismatch surfacing**: fixed a real sign bug (Alpaca reports negative qty for shorts, local ledger stores positive magnitude — the old comparison would have false-flagged every healthy short); now compares magnitudes, logs once per transition into mismatch, counted in `status_report`/CLI (visible, non-blocking — protection legs are evaluated separately). **TIF config hardening**: `PROTECTIVE_ORDER_TIME_IN_FORCE=day` + `ALLOW_DAY_TIF_FOR_MULTIDAY_POSITIONS=false` now rejects at settings load (`SettingsError`) instead of silently degrading every swing hold to day-TIF. **Defensive `None` holding period**: `max_holding_days=None` always resolves to persistent TIF (submission logic + the watchdog's own appropriateness check), regardless of the opt-out flag — unknown is never treated as an informed swing decision. **Incident supersession**: at most one open incident row per position at a time — a position moving between incident types (e.g. `unverifiable → unprotected` once the broker lookup recovers and reveals a real problem) now supersedes the older row instead of leaving two simultaneously-open incidents, which would have let a stale one keep blocking after a human resolved only the newer one (found and fixed while building the escalation feature, not part of the original ask). +21 tests. `alphaos/safety.py`/scanner/AI-labeller/risk/freshness-guard/strategy/approvals/`orchestrator.py`/`__main__.py`/dashboard/`order_manager.py`/`position_manager.py` all verified zero-diff. No scheduler, no live trading, no auto-repair/auto-flatten added.
  - **Opus audit, same PR**: verdict "approve with small fixes" — no blocker/high findings, core escalation/supersession logic verified correct by direct empirical probe (not just tests). Three small fixes applied before merge: (1) added the 1–10 sanity bound on the escalation threshold itself (an unvalidated huge value would have silently disabled escalation — confirmed empirically: `threshold=9999` stayed non-blocking after 5 consecutive failures); (2) added a test proving a stale ack cannot durably unblock an ongoing `unverifiable` position; (3) added a test proving incident supersession leaves exactly one open incident across a type transition. Re-audited after the fixes and approved merge. Remaining follow-ups (non-blocking, tracked in §4): dashboard doesn't yet show `unverifiable`/qty-mismatch tiles (CLI does); qty-mismatch severity is always WARNING regardless of magnitude; no live-gated test confirms Alpaca accepts GTC brackets; `REPLACED` leg state handling; `_simulate_fill` TIF field gap (both carried over from PR2.5, still cosmetic/unreached in practice).
- **Flaky test fixed and merged** (PR #22, `fix/mfe-mae-date-dependent-flake`). `tests/test_mfe_mae.py` was failing 3/22 — confirmed unrelated to PR2.5 (zero-diff on every file involved; reproduced identically on pre-PR2.5 `main`). Root cause: `mock_provider.py` seeds mock prices per `{symbol}:{market_date()}` (deterministic per calendar trading day, by design); `conftest.py`'s `inject_pending_proposal` hardcodes stop=3%/target=6% of entry, which makes `target` **always** algebraically exactly `entry + 2*risk`; three tests used a `+2R` monitor() price probe that can land exactly on that target depending on the day's rounding, closing the position via a target-hit on the first `monitor()` call instead of leaving it open for the intended second probe. That session ran long enough to cross a trading-day boundary and hit exactly this. Fixed: probe at `+1.5R` instead (0.5R margin, verified safe across 400 simulated trading dates) — test-only change.
- **PR2.5 — Broker protection watchdog + multi-day TIF root-cause fix** (PR #21). Implements `docs/roadmap/protection-watchdog.md` in full. **Root-cause fix**: `AlpacaClient.submit_bracket()` no longer hardcodes `time_in_force=day` — `_resolve_tif()` submits `gtc` for any `max_holding_days >= 1` (any hold that can cross a session boundary) unless `ALLOW_DAY_TIF_FOR_MULTIDAY_POSITIONS` explicitly opts back in (default `false`); only the intentionally-intraday `max_holding_days==0` daytrade experiment keeps `day`. **Watchdog**: new `alphaos/execution/protection_watchdog.py` runs every monitor pass (after `reconcile()`, before the local watchdog — ordering matters, avoids false positives on same-pass legitimate closes) and checks every open `alpaca_paper` position's stop/target legs + whether the broker still holds the position at all. Missing stop → `unprotected` (CRITICAL, blocks ALL new entries system-wide). Missing target only (stop still live) → `degraded` (WARNING, non-blocking). Local-open/broker-closed → `closed_mismatch` (CRITICAL, blocks). **Detect + block only** — the watchdog never calls `close_position()`, cancels, or submits anything itself; a human resolves via `alphaos protection_resolve <id> --exit-price X` (closes via the same `close_position()` path every other exit uses, only for `closed_mismatch`) or `alphaos protection_ack <id>` (lifts the block without touching the position). New `alphaos protection_status` CLI, `alphaos status`/dashboard surfacing. +27 tests incl. a direct reproduction of the META incident and a genuine restart-recovery test (file-backed DB, not `:memory:`). Additive schema only (`protection_checks` table, `positions.protection_status` column).
  - **Opus audit + HIGH-1 fix, same PR**: initial audit found one required fix — the TIF boundary was `max_holding_days > 1`, leaving 1-day swings (which CAN cross a session boundary, unlike the 0-day daytrade experiment) still exposed to the exact META failure mode. Fixed to `> 0` in both places the boundary lived. +3 boundary tests. Narrow re-audit confirmed the fix and approved merge.
- **Both audited branches merged to `main`** (2026-07-03). `feat/labeller-failsafe-visibility` → PR #19, then `feat/measurement-foundation` (incl. the Fable 5 review docs + incident/roadmap docs committed as `bee083b`) → PR #20, merged in that order via the GitHub web UI (`main` was at `0248a71`). Re-verified post-merge at the time: 394 passed, 3 skipped, 0 open positions, `real_money_trading=unreachable`, `manual_approval=required`, `kill_switch=off`. No new code changed as part of this merge — integration/verification only. Numerous other stale local feature branches exist (already merged individually over prior sessions) — harmless, cleanup optional (§7).
- **Incident: META protection mismatch, detected + resolved** (2026-07-02). Routine handover verification surfaced that META's broker-side stop (`canceled`) and target (`expired`) orders had both expired at the end of their first trading session (`time_in_force=day` on a multi-day-hold bracket, no scheduler to resubmit) — the position sat naked for ~19 hours while price fell through the original stop, undetected by the local ledger (stale). User confirmed flatten; executed cleanly (0 open orders, 1 position closed, verified read-only after); local ledger manually reconciled via the standard `close_position()` path (not raw SQL) since `OrderManager.reconcile()` was confirmed NOT to detect a non-bracket-leg close — itself a finding, now tracked. Full writeup + timeline: `docs/incidents/2026-07-02-meta-protection-mismatch.md`. Follow-up roadmap item opened: `docs/roadmap/protection-watchdog.md`. No code changed as part of this incident response — detection + manual resolution only.
- **Opus audit fix pass** (`e00595e`, this checkpoint). Opus audited `feat/measurement-foundation` (verdict: APPROVE WITH REQUIRED FIXES) and found two real bugs in the just-built measurement layer. Both fixed on the same branch: **HIGH-1** — forward-return windows were anchored on `candidate_outcomes.created_at_utc` (seed time) instead of the true decision time, which would silently corrupt any backlog-catchup seeding (a candidate decided 30 days ago, seeded today, would have its next bar mislabeled a "1-day" return using a stale price). Fixed with a new additive `decision_at_utc` column sourced from each type's real source row, plus a self-healing repair path for any row missing it. **MEDIUM-1** — MFE/MAE folding could report a positive MAE for a trade that was favorable at every observed point (no anchor). Fixed: entry is now an implicit R=0 observation in both the live and backfill excursion paths (textbook MFE≥0, MAE≤0), while a genuinely stop-less position still correctly returns `(None, None)` rather than a false zero. **NIT-1** — doc comment added. MEDIUM-2 (a minor backfill edge case) deliberately left as follow-up per the audit's own scoping. +18 tests (381 total, was 363). No trading/execution/gate/scheduler behavior touched — see full audit + fix report in this session's transcript if unclear. (This fix's correctness was independently confirmed live during the incident above: the reconciled META exit correctly shows `mfe=0.0` — never favorable at any monitored point — instead of the pre-fix bug's wrong small-negative value.)
- **Measurement foundation — PR1 + PR2** (`eede83c`, this checkpoint, on top of Fable 5's architecture/roadmap review). Fable 5 reviewed AlphaOS end-to-end (packet + response now live at repo root: `FABLE_REVIEW_PACKET.md` / `FABLE_REVIEW_RESPONSE.md`, currently **untracked** — see §7) and recommended measurement before scanner v2/universe expansion/autonomy. Two parts landed: **PR1 (MFE/MAE)** — turned out to be a bug fix, not new work: per-check excursion tracking already existed (`monitoring_snapshots`) but `close_position()` discarded it and wrote a crude `%`-based approximation instead; fixed to fold the exit tick into the real running R-based extremum, plus an idempotent backfill for old closed trades (`alphaos backfill_mfe_mae`). **PR2 (counterfactual outcome ledger)** — new `alphaos/learning/` package + `candidate_outcomes` table: every scanned candidate/proposal/reject/armed-watch/user-override becomes learnable data via 1/3/5-day forward returns and bracket replay, whether or not it became a real trade (NOT a backtest — only replays decisions AlphaOS actually recorded). CLI: `alphaos outcomes_update`, `alphaos outcomes_report`. All additive, `SCHEMA_VERSION` stays 3.
- **Live calibration run + 3 bugfixes** (merged to `main` @ `e381096` — PRs #16/#17/#18). Fixed a labeller output-token-budget bug (`220→800`) that had been silently truncating every real labeller call and blocking ALL live proposals; fixed the live Alpaca catalyst provider (SDK `NewsSet` parsing bug, was silently returning zero catalysts); shipped the User-Override attribution report. Live-fired: **META** long, real Alpaca-paper bracket, filled 41 @ 618.78 — see the incident entry above for how this trade ultimately closed.
- **Labeller fail-safe visibility** (`feat/labeller-failsafe-visibility` — health/status/dashboard/daily-report coverage for labeller output, `alphaos/ai/labeller_health.py`; now merged, see top entry).
- Prior: Roadmap 2.8 (Armed Watch + labeller reasoning + User Override Mode), 2.7 (last30days polarity), 2.6 (gated labeller override), 2.5 (last30days enrichment), 2.4 (catalyst enrichment), 2.3 (interest scanner + AI labelling) — see git log for full history.

---

## 1. Current project state
AlphaOS is a **learning-first, paper-trading "operating system"** on a Mac mini, Python 3.12 venv at `.venv` (uv). `main` is the single line of development, fully clean (442/3/0). Pipeline (shape unchanged since PR2.5): **Scanner → Candidate Packet → AI Labeller → Catalyst/last30days/Polarity enrichment → decision combine → Armed Watch → gates → manual approval (+ User Override layer) → sim/paper execution → monitor/reconcile/exit → protection watchdog (now hardened) → ledger → counterfactual outcome measurement.** Real-money trading remains `unreachable` throughout. Both PR2.5 and PR2.6 are merged; the flaky-test fix is merged. **The one open decision is PR3 (scheduler) — see §9.**

## 2. What was just implemented (this checkpoint)
- **Protection watchdog hardening** (PR2.6, PR #24) — see changelog for full detail. check_error escalation to a new `unverifiable` blocking status; qty-mismatch sign fix + surfacing; TIF config contradiction rejection; defensive `None` holding-period handling; incident supersession (at most one open incident per position).
- Everything from prior checkpoints (PR2.5 protection watchdog, the flaky-test fix, measurement foundation, labeller fail-safe visibility) is merged and unchanged on `main` — see changelog / prior HANDOVER revisions for detail.

## 3. What is working (verified this checkpoint)
- **Full suite: 442 passed, 3 skipped, 0 failed** on `main` @ `0918a55`. PR2.6's own tests: 48/48 (27 from PR2.5 + 21 new).
- Verified via direct empirical probe (not just tests, run independently during the Opus audit): the escalation streak survives the check_error→unverifiable status transition (doesn't reset and de-escalate); a full-pass broker outage (`list_positions()` failing, not just per-order `get_order()`) also escalates correctly; a stale `protection_ack` on an ongoing `unverifiable` position does NOT durably unblock — it re-escalates the very next pass while the broker keeps failing; `unverifiable` cannot be closed via `protection_resolve` (routes to `protection_ack` only — a broker-lookup failure can never cause a wrong-symbol/wrong-reason position close); incident supersession leaves exactly one open incident row across a type transition, with the superseded row still auditable (`resolved_by="watchdog_superseded"`).
- Confirmed the threshold sanity bound: `PROTECTION_CHECK_ERROR_ESCALATION_THRESHOLD` outside 1–10 is rejected at settings load; a pre-fix probe showed `=9999` would have silently kept the watchdog fail-open forever.
- Safety invariants reconfirmed at this checkpoint too: 0 orders/cancels/auto-closes ever originate from the watchdog; `real_trading_guard` fires before the protection check in `execute_proposal()`; `alphaos/safety.py`, scanner, AI-labeller, risk, freshness-guard, strategy, approvals, `orchestrator.py`, `__main__.py`, dashboard, `order_manager.py`, `position_manager.py` all zero-diff against pre-PR2.6 `main`.

## 4. Partially implemented (and what's missing to finish)
- **Protection watchdog cosmetic follow-ups (Opus audit on PR2.6, all non-blocking)**:
  - Dashboard System Health tab doesn't yet show `unverifiable`/qty-mismatch counts as dedicated tiles — the underlying data is fully there (`status_report()` includes it), and `alphaos protection_status` (CLI) already surfaces it completely. Purely a visual gap.
  - Qty mismatch always logs at WARNING regardless of magnitude — no severity grading by how large the divergence is.
  - No live-gated (`RUN_LIVE_ALPACA_TESTS=true`) test confirms the real Alpaca paper API actually accepts a GTC bracket — the whole TIF fix rests on this being true; failure mode is safe (submission would be rejected → `_blocked`, not a silent naked position) but unverified against the real API.
  - A `REPLACED` leg state isn't in `_LIVE_LEG_STATES` — a stop modification (replace) could transiently read as unprotected (false alarm, errs safe, not confirmed against real Alpaca replace behavior).
  - `_simulate_fill` (internal-sim path) doesn't pass a resolved `time_in_force` — harmless (sim positions are never watchdog-checked, no real expiry risk), but inconsistent with the real-paper path.
  - `tif_appropriate`'s handling of `max_holding_days=None` honors the `ALLOW_DAY_TIF_FOR_MULTIDAY_POSITIONS` opt-out flag, while `_resolve_tif` never emits day-TIF for `None` regardless of the flag — a theoretical inconsistency, unreachable in practice (this system can never create a `None`-holding position with day-TIF legs).
- **Attribution report** exists (Roadmap 2.8 follow-up) but has never been exercised on *real* (non-mock) override data — no live overrides have accumulated yet.
- **Cost-model calibration**: still 1/20 real fills (the META entry, now closed). Needs many more real paper fills.
- **MEDIUM-2** (backfill treats a transient empty-bars fetch as permanently `unavailable`) — deliberately left as a fast-follow per the measurement-foundation audit's own scoping; not yet done.
- **Measurement data has near-zero real volume yet**: the counterfactual ledger works correctly but has only been run against mock scans and hand-built fixtures — no real scheduled cadence exists to actually accumulate it (that's PR3, not started).

## 5. Not done yet (deferred / future)
Per the Fable 5 review's PR-sized roadmap (`FABLE_REVIEW_RESPONSE.md`), none of these are started: **PR3 scheduler v1.5** (the next recommended step, and now unblocked — daily scan+monitor+outcomes_update+digest; should call `run_monitor_once()`, which already runs reconcile→watchdog→local-monitor in the correct order, with the watchdog now hardened against unverifiable-protection-state fail-open), decision-lineage stamping, earnings-proximity flag, proposal TTL, TQS v0, attribution v2 (ΔR), portfolio concentration monitor, playbook registry v0, generalized anomaly monitor, NightDesk export. Also still open from before: real Alpaca paper execution beyond the one (now-closed) META trade, universe expansion (deliberately deferred — shadow-rank only per the review), the mock-mode-doesn't-hard-disable-last30days footgun (§7.4).

## 6. Test results
- **On `main` @ `0918a55` right now: 442 passed, 3 skipped, 0 failed.** Clean.
- Skips = `tests/test_live_alpaca.py` (gated behind `RUN_LIVE_ALPACA_TESTS=true`). Fully hermetic otherwise.
- `tests/test_protection_watchdog.py`: 48 tests total. 27 from PR2.5 (META reproduction, TIF boundary at every holding-day value, reconcile-before-watchdog ordering, alert dedup, restart-recovery against a file-backed DB, human-only resolution never auto-close). +21 from PR2.6 (check_error single-failure visibility, escalation to `unverifiable` at threshold, stale-ack re-block, full-outage escalation, qty-mismatch long/short/sign-correctness, TIF config contradiction rejection, `None`-holding-period defaults, incident supersession, no-orders/no-auto-close during escalation).

## 7. Known risks / blockers
1. **Stale local feature branches**: many older local branches (`feat/alpaca-paper-validation`, `feat/cost-model-calibration`, `feat/interest-scanner-ai-labelling`, `feat/protection-watchdog`, `feat/protection-watchdog-hardening`, `fix/mfe-mae-date-dependent-flake`, `docs/handover-pr2-5-checkpoint`, etc. — already merged individually) are still sitting in local `git branch -vv`. Harmless (all content is in `main`); safe to `git branch -d` at your discretion, not done automatically.
2. **Stray WAL files**: `data/demo-chain.db-shm` / `-wal` — pre-existing, untouched, harmless; clean up at your discretion.
3. **Chain cost**: every real `interest_scan` still costs OpenAI money; `LAST30DAYS_ENABLED`/`POLARITY_ENABLED` are on in `.env`.
4. **Operational footgun (documented, not fixed)**: `ALPHAOS_MODE=mock` does NOT disable `LAST30DAYS_ENABLED=true`, causing subprocess hangs during ad-hoc CLI testing. Always pair mock-mode testing with explicit `LAST30DAYS_ENABLED=false LAST30DAYS_POLARITY_ENABLED=false EXECUTION_PROVIDER=simulated_internal`. Still not fixed.
5. **Cannot push to `main` from this environment** — feature branches push fine over the SSH deploy key; any future `main`-bound change needs a PR + GitHub web UI merge, same pattern as every change this session.
6. **Mock market data is date-seeded** (`{symbol}:{market_date()}`) — by design, for reproducibility, but it means any test that derives exact price/R-multiple boundaries from mock prices can silently re-break on some future date if it re-introduces an exact-boundary probe (this is what broke `test_mfe_mae.py`). Leave a margin, don't probe exactly at a computed boundary.
7. **Protection watchdog cosmetic follow-ups** (§4) — none are urgent/blocking; the dashboard gap is the most visible one if you want it closed before PR3.

## 8. Exact commands to run next
```bash
cd "/Users/ck/Documents/Claude Playground/AlphaOS"

# confirm the account is actually clean (read-only) before doing anything else
.venv/bin/python -c "
from alphaos.config.settings import load_settings
from alpaca.trading.client import TradingClient
s = load_settings(); tc = TradingClient(s.alpaca_api_key, s.alpaca_secret_key, paper=True)
print('open positions:', tc.get_all_positions())
"   # expect: []

# verify code state
.venv/bin/python -m pytest                 # expect: 442 passed, 3 skipped, 0 failed
git status -sb && git log --oneline | head -6
git branch --show-current                  # expect: main

# broker protection watchdog status (new this checkpoint)
.venv/bin/python -m alphaos protection_status

# operate (chain is LIVE -> real OpenAI calls each run; use ALPHAOS_MODE=mock for safe testing)
.venv/bin/python -m alphaos status
.venv/bin/python -m alphaos outcomes_update        # seed + resolve counterfactual outcomes
.venv/bin/python -m alphaos outcomes_report        # measurement visibility (no statistical claims)
.venv/bin/python -m alphaos backfill_mfe_mae       # backfill any legacy closed trades

# SAFE mock testing (avoids the last30days subprocess-hang footgun, §7.4):
ALPHAOS_MODE=mock EXECUTION_PROVIDER=simulated_internal LAST30DAYS_ENABLED=false \
  LAST30DAYS_POLARITY_ENABLED=false ALPHAOS_DB_PATH=data/demo.db .venv/bin/python -m alphaos interest_scan
```

## 9. Recommended next prompt (paste into a fresh window)
```
Read HANDOVER.md in the AlphaOS repo first (single source of truth). Note the protection
watchdog is now fully built AND hardened -- PR2.5 (PR #21) + PR2.6 (PR #24) both merged
to main, each audited by Opus and approved, plus a small unrelated flaky-test fix
(PR #22). main is clean: 442 passed, 3 skipped, 0 failed. No merge decisions pending.

Verify code state: `.venv/bin/python -m pytest` (expect 442/3/0), confirm branch is
`main` @ `0918a55` or later, and confirm the paper account has 0 open positions (§8 has
the read-only command).

The one real open decision: is it time to start PR3 (scheduler v1.5,
FABLE_REVIEW_RESPONSE.md §16)? The main argument for waiting was "don't automate on top
of a watchdog that fails open on unverifiable protection" -- that's now fixed (PR2.6).
The remaining follow-ups (§4) are all cosmetic/non-blocking. If you want to start PR3,
help me scope it (daily scan+monitor+outcomes_update+digest cadence, calling the
existing run_monitor_once() which already runs reconcile->watchdog->local-monitor
correctly). If you'd rather close out the cosmetic follow-ups first (dashboard tiles for
unverifiable/qty-mismatch is the most visible one), that's fine too -- your call.

Do NOT start new feature work until we've discussed which one first.

Hard constraints (HANDOVER §10): real-money stays unreachable; manual approval
non-bypassable; no AI/catalyst/last30days/polarity/measurement output bypasses gates;
migrations additive only; keep tests green; the counterfactual outcome ledger AND the
protection_checks table are measurement/audit-only and must never be read by any
gate/eval/labeller/risk/execution path; the protection watchdog detects + blocks only,
never auto-closes/cancels/submits; PROTECTION_CHECK_ERROR_ESCALATION_THRESHOLD must stay
validated 1-10.
```

## 10. Anything the next session must NOT change (hard invariants)
- **Real-money trading stays unreachable.** `REAL_TRADING_ENABLED=false`, `ALLOW_REAL_ORDERS=false`; `ALPHAOS_MODE=live` rejected. Do not touch `safety.py`. `system_health()["real_money_trading"]` must remain `"unreachable"`.
- **Manual approval is the default and non-bypassable** (`APPROVAL_MODE=manual`). No path may auto-submit or skip approval. `high_risk_narrative` proposals are manual-only regardless of approval mode.
- **No AI/catalyst/last30days/polarity/measurement output bypasses gates.** Freshness, spread, liquidity, crossed-quote, risk, sizing, daily-cap, exposure, kill switch, stop/target, market-session, price-drift gates are authoritative.
- **AI category label is ADVISORY; the override is gated + symmetric.** Default downgrade-only. When ARMED it may move the decision UP or DOWN, gated + audited (`decision_adjustments`).
- **Polarity is CONTEXT that can ARM, never EXECUTE.** Deterministic AlphaOS-side arming only; fails safe to non-arming.
- **User Override (2.8) is a SEPARATE decision layer; NEVER rewrites AlphaOS's recommendation**, never bypasses gates/approval/real-money guard, never auto-executes.
- **The counterfactual outcome ledger (`candidate_outcomes`) is PURE MEASUREMENT.** It must never be read by any gate, eval, labeller, risk check, or execution path — write-only from `alphaos/learning/`. `decision_at_utc` must stay the anchor for all forward-window math (never revert to seed-time anchoring — Opus audit HIGH-1 on the measurement-foundation PR, a real bug, not a style choice).
- **MFE/MAE stay textbook-anchored** (entry = implicit R=0; MFE≥0, MAE≤0 always) — do not revert to the old unanchored fold (Opus audit MEDIUM-1 on the measurement-foundation PR).
- **The broker protection watchdog (`alphaos/execution/protection_watchdog.py`) is DETECT + BLOCK ONLY.** It must never call `close_position()`, cancel an order, or submit an order itself. Resolution is human-triggered only, via `protection_resolve` (closed_mismatch only, requires an operator-confirmed exit price, uses the same `close_position()` path as every other exit) or `protection_ack` (unprotected/degraded/unverifiable, never touches the position). Do not add auto-repair/auto-flatten without explicit new intent — this was a deliberate design decision (Opus-reviewed), not an oversight.
- **Multi-day/swing TIF policy**: any `max_holding_days >= 1` proposal must submit persistent (`gtc`) protective legs by default; only `max_holding_days==0` (pure intraday) may default to `day`; `max_holding_days=None` (unknown) must ALSO default to persistent, regardless of the opt-out flag. `ALLOW_DAY_TIF_FOR_MULTIDAY_POSITIONS` must stay `false` by default — this is the fix for the 2026-07-02 META incident's root cause (Opus audit HIGH-1 got this exact boundary wrong once already at `>1`; do not reintroduce that mistake). `PROTECTIVE_ORDER_TIME_IN_FORCE=day` + `ALLOW_DAY_TIF_FOR_MULTIDAY_POSITIONS=false` is a rejected config combination (settings load raises) — do not silently auto-correct it instead of rejecting.
- **NEW — check_error escalation (PR2.6)**: a single broker per-order lookup failure is WARNING-only and non-blocking; `PROTECTION_CHECK_ERROR_ESCALATION_THRESHOLD` (validated 1–10, default 3) consecutive failures for the same position escalate to `unverifiable` — CRITICAL, blocks new entries exactly like `unprotected`. Do not remove the 1–10 validation bound (an unvalidated huge value silently reintroduces fail-open — confirmed empirically during the PR2.6 audit).
- **NEW — incident supersession (PR2.6)**: at most one open (`resolved_at_utc IS NULL`) incident row may exist per position at a time. A transition between incident types must resolve (`resolved_by="watchdog_superseded"`) the prior open incident before opening a new one — never leave two simultaneously-open incident rows for the same position, or a human resolving only the newer one leaves the stale older one still blocking.
- **last30days is a SEPARATE layer; no vendoring.** Real-AI calls use `max_completion_tokens` (NOT `max_tokens`).
- **`LABEL_MAX_OUTPUT_TOKENS` must stay ≥512** (guarded by a test) — 220 silently truncated every real label and blocked all live proposals for an unknown period; do not lower it.
- **Execution = `simulated_internal`** unless deliberately enabling opt-in `alpaca_paper` (paper-only, explicit intent).
- **Migrations additive only.** `SCHEMA_VERSION` stays 3 for additive changes.
- **Audit/evidence writes never gate execution/exit paths** (best-effort, after the action).
- **Dashboard stays read-only on render**; do not expose on the network without auth.
- **Mock market prices are seeded per `{symbol}:{market_date()}`** (deterministic per calendar trading day, not fixed forever) — new tests that derive exact price/R-multiple boundaries from mock prices must leave a margin (e.g. probe at 1.5R, not exactly 2R when a 2:1 target is in play), or they can silently re-break on some future date the way `test_mfe_mae.py` just did.
- Do not change OpenAI decision logic / risk/freshness thresholds / bracket-OCO-watchdog exits / Alpaca submission (beyond the TIF policy above) without explicit intent.
