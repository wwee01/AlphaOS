# AlphaOS exact-version lockfile (PR9.5).
#
# requirements.txt states the loose, human-intent dependency set (what's
# needed and why). This file is the EXACT frozen snapshot of what's actually
# installed in the working .venv on the production Mac mini, captured
# 2026-07-06 -- so the venv can be rebuilt identically after a disk loss,
# machine replacement, or accidental `rm -rf .venv`, rather than hoping loose
# `>=` pins in requirements.txt happen to resolve the same way months later.
#
# Regenerate after any deliberate dependency change:
#   uv pip freeze --python .venv/bin/python | grep -v '^-e file://' > requirements-lock.txt
#
# Rebuild the venv from this file:
#   uv venv && uv pip install --python .venv/bin/python -r requirements-lock.txt
#   uv pip install --python .venv/bin/python -e .   # the project itself, editable
#
# This is a full transitive closure (includes streamlit/pandas/numpy/etc.
# pulled in by the live-mode extras), not just the direct dependencies named
# in requirements.txt / pyproject.toml.

alpaca-py==0.43.4
altair==6.2.1
annotated-types==0.7.0
anyio==4.14.0
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.4
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.4.1
distro==1.9.0
gitdb==4.0.12
gitpython==3.1.50
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
itsdangerous==2.2.0
jinja2==3.1.6
jiter==0.16.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
markupsafe==3.0.3
msgpack==1.2.1
narwhals==2.22.1
numpy==2.5.0
openai==2.44.0
packaging==26.2
pandas==3.0.3
pillow==12.2.0
pluggy==1.6.0
protobuf==7.35.1
pyarrow==24.0.0
pydantic==2.13.4
pydantic-core==2.46.4
pydeck==0.9.2
pygments==2.20.0
pytest==9.1.1
python-dateutil==2.9.0.post0
python-multipart==0.0.32
pytz==2026.2
referencing==0.37.0
requests==2.34.2
rpds-py==2026.5.1
six==1.17.0
smmap==5.0.3
sniffio==1.3.1
sseclient-py==1.9.0
starlette==1.3.1
streamlit==1.58.0
tenacity==9.1.4
toml==0.10.2
tqdm==4.68.3
typing-extensions==4.15.0
typing-inspection==0.4.2
tzdata==2026.2
urllib3==2.7.0
uvicorn==0.49.0
websockets==16.0
