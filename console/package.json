{
  "name": "alphaos-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AlphaOS operator console (ND-1: read-only Tonight cockpit) -- React frontend for alphaos/api's FastAPI backend.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16",
    "vitest": "^3.2.4"
  }
}
